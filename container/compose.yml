# https://docs.docker.com/reference/compose-file/
services:
  zju-os-tool:
    image: git.zju.edu.cn:5050/os/tool:latest
    container_name: zju-os-tool
    hostname: zju-os-tool
    volumes:
      - ./:/zju-os/tool
    working_dir: /zju-os/tool
    init: true
    entrypoint: ["sleep", "infinity"]
